  {
    "name": "real-time-whiteboard-platform",
    "version": "1.0.0",
    "dependencies": {
    "express": "^4.18.2"
  },
    "scripts": {
      "build": "cd server/project && npm install && npm run build",
      "start": "cd server && node server.js"
    },
    "engines": {
      "node": "22.x"
    }
  }
